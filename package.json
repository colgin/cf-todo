{
  "scripts": {
    "dev": "wrangler dev src/index.tsx --port=56034",
    "deploy": "pnpm -C site run build && wrangler deploy --minify src/index.tsx"
  },
  "dependencies": {
    "hono": "^3.7.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231002.0",
    "wrangler": "^3.9.0"
  }
}
